% ============================================================
%  ELITE DANGEROUS: Справочник исследователя
%  Компилятор: XeLaTeX (Overleaf: Menu -> Compiler -> XeLaTeX)
%  Overleaf компилирует автоматически дважды — перекомпиляция
%  не требуется вручную.
% ============================================================

\documentclass[9pt]{extarticle}

% ── XeLaTeX: Unicode + шрифты с кириллицей ──────────────────
\usepackage{fontspec}
\defaultfontfeatures{Ligatures=TeX, Scale=MatchLowercase}
% CMU (Computer Modern Unicode) — полная кириллица, есть в TeX Live
\setmainfont{CMU Sans Serif}
\setsansfont{CMU Sans Serif}
\setmonofont{CMU Typewriter Text}

% ── Языки (babel работает с XeLaTeX + fontspec) ──────────────
\usepackage[russian,english]{babel}

% ── Страница (телефон: 1220×2712 px @ 446 ppi = 69×140 мм) ──
\usepackage[
  paperwidth  = 69mm,
  paperheight = 140mm,
  top         = 3.5mm,
  bottom      = 7mm,
  left        = 3mm,
  right       = 3mm,
  headheight  = 4mm,
  headsep     = 1.5mm,
  footskip    = 4.5mm
]{geometry}

% ── Микротипография ──────────────────────────────────────────
\usepackage{microtype}

% ── Цвета (xcolor с table-опцией загружает colortbl сам) ─────
\usepackage[table,dvipsnames]{xcolor}
\definecolor{edbg}       {HTML}{0D0800}
\definecolor{edorange}   {HTML}{E07820}
\definecolor{edorangedim}{HTML}{7A3A08}
\definecolor{edtext}     {HTML}{D8CCBA}
\definecolor{edblue}     {HTML}{6699EE}
\definecolor{edgray}     {HTML}{666666}
\definecolor{edgraytext} {HTML}{999999}
\definecolor{edtablebg}  {HTML}{150C00}
\definecolor{edtablealt} {HTML}{1C1200}
\definecolor{edheaderbg} {HTML}{2A1500}
\definecolor{edboxbg}    {HTML}{160E00}
\definecolor{edgreen}    {HTML}{44CC66}
\definecolor{edred}      {HTML}{CC4444}
\definecolor{edselect}   {HTML}{C06010}

% ── Тёмный фон ───────────────────────────────────────────────
\usepackage{pagecolor}
\pagecolor{edbg}
\color{edtext}

% ── Гиперссылки (после xcolor!) ──────────────────────────────
\usepackage[
  colorlinks = true,
  linkcolor  = edorange,
  urlcolor   = edblue,
  pdftitle   = {ELITE DANGEROUS: Справочник исследователя},
  pdfauthor  = {Commander},
  bookmarks  = true,
  unicode    = true
]{hyperref}

% ── Колонтитулы ──────────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.2pt}
\renewcommand{\headrule}{\hbox to\headwidth{%
  \color{edorangedim}\leaders\hrule height\headrulewidth\hfill}}
\renewcommand{\footrulewidth}{0.2pt}
\renewcommand{\footrule}{\hbox to\headwidth{%
  \color{edorangedim}\leaders\hrule height\footrulewidth\hfill}}

\fancyhead[L]{{\color{edorangedim}\fontsize{5}{6}\selectfont
  ELITE DANGEROUS}}
\fancyhead[R]{{\color{edgray}\fontsize{5}{6}\selectfont\thepage}}
\fancyfoot[C]{%
  \hyperlink{toc}{{\color{edorangedim}\fontsize{5}{6}\selectfont
    \textbf{\^{}}~К~СОДЕРЖАНИЮ}}}

\fancypagestyle{empty}{
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0pt}
}

% ── Слои: \toggletrue = включён, \togglefalse = скрыт ────────
\usepackage{etoolbox}
\newtoggle{showMaterials}   \toggletrue{showMaterials}
\newtoggle{showEngineers}   \toggletrue{showEngineers}
\newtoggle{showModules}     \toggletrue{showModules}
\newtoggle{showNavigation}  \toggletrue{showNavigation}
\newtoggle{showExobiology}  \toggletrue{showExobiology}

% ── TColorBox ────────────────────────────────────────────────
\usepackage[most]{tcolorbox}

\tcbset{
  edbox/.style={
    colback=edboxbg, colframe=edorangedim, coltext=edtext,
    boxrule=0.3mm, arc=0mm,
    left=1.5mm, right=1.5mm, top=1mm, bottom=1mm,
    fontupper=\fontsize{7}{8.5}\selectfont,
    before skip=1.5mm, after skip=1.5mm,
    breakable, enhanced,
  },
  edbox-titled/.style={
    edbox, colframe=edorange,
    coltitle=edbg, colbacktitle=edorange,
    fonttitle=\bfseries\fontsize{7}{8}\selectfont,
    attach boxed title to top left,
    boxed title style={arc=0mm, boxrule=0pt,
      left=1.5mm, right=1.5mm, top=0.4mm, bottom=0.4mm},
    top=4mm,
  },
  edbox-blue/.style={
    edbox, colframe=edblue,
    coltitle=edbg, colbacktitle=edblue,
    fonttitle=\bfseries\fontsize{7}{8}\selectfont,
    attach boxed title to top left,
    boxed title style={arc=0mm, boxrule=0pt,
      left=1.5mm, right=1.5mm, top=0.4mm, bottom=0.4mm},
    top=4mm,
  },
  edbox-grade/.style={
    edbox, colframe=edselect, colbacktitle=edselect,
    coltitle=edbg,
    fonttitle=\bfseries\fontsize{6.5}{8}\selectfont,
    attach boxed title to top right,
    boxed title style={arc=0mm, boxrule=0pt,
      left=1mm, right=1mm, top=0.4mm, bottom=0.4mm},
    top=1mm,
    breakable=false,
  },
}

% ── Заголовки разделов ────────────────────────────────────────
\usepackage{titlesec}

\titleformat{\section}
  {\color{edorange}\fontsize{9}{11}\selectfont\bfseries\MakeUppercase}
  {}{0pt}
  {\vspace{0.5mm}\color{edorangedim}\hrule\vspace{1mm}\color{edorange}}
  [\vspace{1mm}{\color{edorangedim}\hrule}\vspace{2mm}]
\titlespacing*{\section}{0pt}{5mm}{2mm}

\titleformat{\subsection}
  {\color{edorange}\fontsize{7.5}{9.5}\selectfont\bfseries}
  {}{0pt}{}
  [\vspace{0.3mm}{\color{edorangedim}\rule{\linewidth}{0.2pt}}]
\titlespacing*{\subsection}{0pt}{3mm}{1.5mm}

\titleformat{\subsubsection}
  {\color{edgraytext}\fontsize{7}{8.5}\selectfont\bfseries}
  {}{0pt}{}[]
\titlespacing*{\subsubsection}{0pt}{2mm}{1mm}

% ── Таблицы ──────────────────────────────────────────────────
\usepackage{tabularx}
\usepackage{booktabs}
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.15}

% ── Списки ───────────────────────────────────────────────────
\usepackage{enumitem}
\setlist[itemize]{
  leftmargin=3.5mm, itemsep=0.5pt, parsep=0pt, topsep=1mm,
  label={\color{edorange}\fontsize{6}{7}\selectfont$\bullet$},
}
\setlist[enumerate]{
  leftmargin=4.5mm, itemsep=0.5pt, parsep=0pt, topsep=1mm,
}

% ── Вспомогательные команды ──────────────────────────────────
% Используем \ttfamily вместо \fontfamily{lmtt} для XeLaTeX
\newcommand{\edcoord}[1]{%
  {\color{edblue}\fontsize{7}{8.5}\selectfont\ttfamily #1}}
\newcommand{\edval}[1]{{\color{edorange}\bfseries #1}}
\newcommand{\edlabel}[1]{%
  {\color{edgray}\fontsize{6.5}{8}\selectfont #1}}
\newcommand{\eddim}[1]{%
  {\color{edgraytext}\fontsize{6.5}{8}\selectfont\itshape #1}}
\newcommand{\edgrade}[1]{%
  \colorbox{edselect}{%
    \color{edbg}\bfseries\fontsize{6}{7}\selectfont~G#1~}}
\newcommand{\edcr}[1]{%
  {\color{edgreen}\fontsize{7}{8.5}\selectfont #1~CR}}
\newcommand{\edrule}{%
  \vspace{0.5mm}%
  {\color{edorangedim}\rule{\linewidth}{0.2pt}}%
  \vspace{0.5mm}}
\newcommand{\edsep}{%
  \vspace{1mm}%
  {\color{edorangedim}\rule{\linewidth}{0.3pt}}%
  \vspace{1mm}}

% Поле: значение (с переносом строки)
\newcommand{\edfield}[2]{%
  \edlabel{#1:}~{\fontsize{7}{8.5}\selectfont #2}\par}
\newcommand{\edcoordfield}[2]{%
  \edlabel{#1:}~\edcoord{#2}\par}

% ── Перенос и переполнение ────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{1mm}
\emergencystretch=4em
\tolerance=1500
\hbadness=10000

% ============================================================
\begin{document}
% ============================================================

\thispagestyle{empty}
\input{chapters/00-title}

\iftoggle{showMaterials}{%
  \clearpage
  \input{chapters/01-materials}
}{}

\iftoggle{showEngineers}{%
  \clearpage
  \input{chapters/02-engineers}
}{}

\iftoggle{showModules}{%
  \clearpage
  \input{chapters/03-modules}
}{}

\iftoggle{showNavigation}{%
  \clearpage
  \input{chapters/04-navigation}
}{}

\iftoggle{showExobiology}{%
  \clearpage
  \input{chapters/05-exobiology}
}{}

\end{document}
